# Lock dependencies to avoid breaking changes
lockfile-version=2

# Use package-lock for deterministic builds
package-lock=true

# Disable strict SSL for CI (optional)
# strict-ssl=false

# Save exact versions
save-exact=true

# Disable progress bar for cleaner logs
progress=false

# Set npm registry
registry=https://registry.npmjs.org/